INDEX: 359
ID: 556889906
TITLE: Standard-SSO - Expression Glossary
PARENT: Standard-SSO
UPDATED: 2026-05-06
URL: https://confluence.alkami.com/spaces/SDKC/pages/556889906
-----
## Overview

The following page will detail out all of the built-in expressions available through configuration to the Standard SSO microservice.

## Expressions

| Name | Example | Description |
| AccountDisplayName | {{ AccountDisplayName }} | Returns the display name of the account. |
| AccountExtendedProperty | {{ AccountExtendedProperty('PropertyName') }} | Allows for looking up an extended property associated to an account.Arguments:#1 - A constant string that refers to the Extended Property name.Notes:Requires the user to select an account. |
| AccountHolder | {{ AccountHolder }} | Returns the account's holder number.Notes:Requires the user to select an account. |
| AccountNumber | {{ AccountNumber }} | Returns the account's account number (unmasked).Notes:Requires the user to select an account. |
| AddressLine1 | {{ AddressLine1 }} | Returns the first address line of the user's address.Notes:The primary contact is preferred, if available. |
| AddressLine2 | {{ AddressLine2 }} | Returns the second address line of the user's address.Notes:The primary contact is preferred, if available. |
| Append | {{ Append('A', 'B', AccountHolder) }} | Appends two or more expressions into a single string.Arguments:Variable - Two or more arguments.Notes:Arguments can be constants or expressions. |
| Base64Encode | {{ Base64Encode('A') }} | Encodes a string into Base64Arguments:#1 - A constant string or an expression that will be encoded to base64 |
| Base64Decode | {{ Base64Decode('A') }} | Decodes a base64 string into a UTF-8 stringArguments:#1 - A constant string or an expression that will be decode to UTF-8 |
| BirthDate | {{ BirthDate('MM-dd-yyyy') }} | Returns the user's birth date (if available) formatted with the provided format string.Arguments:#1 - A constant string that is the format string for the user's birth date.Notes:The format string must adhere to C# DateTime format standards. |
| CardActivationDate | {{ CardActivationDate('MM-dd-yyyy') }} | Returns the card's activation date (if available) formatted with the provided format string.Arguments:#1 - A constant string that is the format string for the card's activation date.Notes:The format string must adhere to C# DateTime format standards.Requires the user to select a card. |
| CardDescription | {{ CardDescription }} | Returns the card's description.Notes:Requires the user to select a card. |
| CardExpirationDate | {{ CardExpirationDate('MM-dd-yyyy') }} | Returns the card's expiration date (if available) formatted with the provided format string.Arguments:#1 - A constant string that is the format string for the card's expiration date.Notes:The format string must adhere to C# DateTime format standards.Requires the user to select a card. |
| CardForeignTransactionsEnabled | {{ CardForeignTransactionsEnabled }} | Returns a value indicating if the cards foreign transactions are enabled using Card Management. |
| CardHolderDisplayName | {{ CardHolderDisplayName }} | Returns the card holder display name using Card Management.Notes:Requires the user to select a card. |
| CardIsActivated | {{ CardIsActivated }} | Returns if the card is activated using Card Management. |
| CardIsBlocked | {{ CardIsBlocked }} | Returns if the card is blocked using Card Management. |
| CardIsChipped | {{ CardIsChipped }} | Returns if the card is chipped using Card Management. |
| CardIsRegistered | {{ CardIsRegistered }} | Returns if the card is registered using Card Management. |
| CardNumber | {{ CardNumber }} | Returns the card's number using Card Management.Notes:*** Different from expression CreditCardNumber which is the retrieved credit card number associated with an account.Requires the user to select a card. |
| CardPinUpdateAttemptsLeft | {{ CardPinUpdateAttemptsLeft }} | Returns the number of card pin update attempts left using Card Management. |
| CardType | {{ CardType }} | Returns the card's type (unknown/debit/credit).Notes:Requires the user to select a card. |
| City | {{ City }} | Returns the city of the user's address.Notes:The primary contact is preferred, if available. |
| Coalesce | {{ Coalesce(City, State, 'Unknown') }} | Returns the first non-null/empty/white-space expression.Arguments:Variable - Two or more arguments.Notes:Arguments can be constants or expressions. |
| CombineBase64Strings | {{ CombineBase64Strings('Base64String1', 'Base64String2', Encrypt('EncryptName', SomeInputWhichWillEncryptToABase64String)) }} | Combines two or more expressions (which must evaluate to base64 strings) into a single base64 string. Arguments:Variable - Two or more arguments.Notes:Arguments can be constants or expressions, but must be either constant base64 strings or expressions that evaluate to base64 strings. The end result does not append the strings together, it converts each argument to a byte array, combines the arrays into a single byte array. then converts the single byte array into a base64 string. |
| CoreName | {{ CoreName }} | Returns the core name of the account type for the account. |
| Country | {{ Country }} | Returns the country of the user's address.Notes:The primary contact is preferred, if available. |
| CreditCardNumber | {{ CreditCardNumber }} | Returns the account's credit card number (if applicable) for an account.Notes:Requires the user to select an account. |
| CreditLimitAmount | {{ CreditLimitAmount }} | Returns the account's credit limit amount (if applicable).Notes:Requires the user to select an account. |
| DateTimeOffset | {{ DateTimeOffset('MM-dd-yyyy', 'Day', 3, 'yyyyMMdd') }} | Returns a calculated Datetime. Arguments:#1 - Datetime to calculate offset from#2 - Enum used to add/subtract from datetimeYearMonthDayMinuteSecond#3 - Integer used for calculation#4 - Output datetime format |
| DisplayName | {{ DisplayName }} | Returns the user's display name. |
| DeriveKey | {{ DeriveKey( 'DeriveKeyName', 'Secret', 16, 32 ) }} | Derives a key for the given secret, salt, key size, and optionally, offset. See the "Derive Key Example" on Standard-SSO.Arguments:#1 - A constant string that refers to the DeriveKey's Key.#2 - The secret used to derive the key.#3 - The desired key size.#4 - Optional - if provided, the key will be offset by this size. For example for key size of 16 with an offset of 32, a key with a length of 16 bytes will be derived after being offset by 32 bytes. |
| Email | {{ Email }} | Returns the user's email address.Notes:The primary contact is preferred, if available. |
| Encrypt | {{ Encrypt('EncryptName', 'Input' }} | Returns the encrypted string, using the named encrypt method. See the "Encrypt Example" on Standard-SSO.Arguments:#1 - A constant string that refers to the EncryptMethod's Key.#2 - A constant or an expression that will be encrypted. |
| FirstName | {{ FirstName }} | Returns the user's first name. |
| Format | {{ Format('{0} {1}', 'Hello', 'World') }} | Returns a copy of the format in which any format items are replaced by the string representation of the additional string parameters.Arguments:#1 - A constant string that is the format string.#2 .. n - The strings to format.Notes:See the method Format(String, Object[]) from String.Format Method for details on the implementation. |
| FormatDateTime | {{ FormatDateTime('MM-dd-yyyy') }}-or-{{ FormatDateTime('MM-dd-yyyy', 'Central Standard Time') }} | Returns the current UTC DateTime formatted with the provided format string.Arguments:#1 - A constant string that is the format string for the UTC DateTime.#2 - (Optional) A constant string that is the Timezone Id.#3 - For ISO 8601 DateTime formatNotes:The format string must adhere to C# DateTime format standards.Acceptable ISO 8601 DateTime formats are: {{ FormatDateTime('O'}}{{ FormatDateTime('yyyy'-'MM'-'dd'T'HH':'mm':'ss'.'fffffffK'}}https://docs.microsoft.com/en-us/dotnet/standard/base-types/standard-date-and-time-format-strings |
| FormatNumber | {{ FormatNumber('000-00-0000', TaxId) }} | Returns a string representation of the numeric value provided as specified by the format.Arguments:#1 - A constant string that is the format string.#2 - The numeric value to format.Notes:The format must adhere to the following:Standard numeric format stringsCustom numeric format strings |
| Guid | {{ Guid }} | Returns a random Guid.Notes:The format of the Guid is: XXXXXXXX-XXXX-XXXX-XXXX-XXXXXXXXXXXX |
| Hash | {{ Hash('HashName', 'Input' }} | Returns the hashed string, using the named hash method. Supported hash algorithms include SHA512, SHA256, AND SHA384. See the "Hash Example" on Standard-SSO.Arguments:#1 - A constant string that refers to the HashMethod's Key.#2 - A constant or an expression that will be hashed.Notes:Hex and Base64 Hash encodings are supported. Refer to HexEncode and Base64Encode expression names. |
| HexEncode | {{ HexEncode('A') }} | Encodes a string into a Hex stringArguments:#1 - A constant string or an expression that will be encoded to a Hex string |
| HexDecode | {{ HexDecode('A') }} | Decodes a Hex string into a UTF-8 stringArguments:#1 - A constant string or an expression that will be decode to UTF-8 |
| HomePhone | {{ HomePhone }} | Returns the user's home phone.Notes:The primary contact is preferred, if available. |
| Http | {{ Http('ServiceName') }} | Returns the result of the HTTP service call. See the "Http Service Example" on Standard-SSO.Arguments:#1 - A constant string that refers to the HTTP Service's Key. |
| IpAddress | {{ IpAddress }} | Returns the user's requesting IPAddress.Notes:The format of the response is: AAAA.BBBB.CCCC.DDDD |
| JsonPath | {{ JsonPath('{topLevelObject: {secondLevelValue: a} }', '$.topLevelObject.secondLevelValue') }} | Returns the value from the specified path in a JSON ObjectArgumentsJSON Object (e.g. from an HTTP service)Path to value desired. JsonPath format (See SelectToken with JSONPath)NotesPrimary use case would be for reusing objects returned from http service calls. See MessagePay for Loan Payment for an example of this being done. |
| Jwt | {{ Jwt('ServiceName') }} | Returns the token for the JWT service call. See the "JWT Service Example" on Standard SSO.Arguments1 - A constant string that refers to the JWT Service's Key.NotesRefer Standard SSO-JWT for more details |
| Jwt sub expression | {{ Jwt('ServiceName', AnotherExpressionOrValue) }} | Returns a JWE of the passed in value given the JWE methods defined in the service. Notes:This can only be used for the JWE service. This will use all the configuration as defined and then overwrite the payload to be a string value before encrypting.Standard-SSO - JWT#FieldLevelEncryptionExample |
| LastName | {{ LastName }} | Returns the user's last name. |
| LateChargeAmountDue | {{ LateChargeAmountDue }} | Returns the account's late charge amount (if applicable).Notes:Requires the user to select an account. |
| Left | {{ Left('Input', 4) }} | Returns the left-most characters from the provided string.Arguments:#1 - A constant or an expression that will be modified.#2 - A constant string that indicates how many characters will be returned.The value must be greater than zero.Notes:If the string is less than the number of desired characters, the string is returned as is. |
| Lower | {{ Lower('Input') }} | Returns the lowercase version of the provided string.Arguments:#1 - A constant or an expression that will be modified. |
| Mask | {{ Mask('Input') }}-or-{{ Mask('Input', '#', 4, 8) }} | Returns the input string masked.Arguments:#1 - A constant or an expression that will be masked.#2 - (Optional) A constant string of a single character that will be used to mask (Default: *).#3 - (Optional) A constant string representing the number of characters on the right to keep (Default: 4).The value must be greater than zero.#4 - (Optional) A constant string representing the total number of characters to return for the final string (Default: 0).The value must be zero or greater than the number of characters to keep (Argument #3).Notes:If total number of characters is zero, the string's original size will not be limited. |
| MaskedAccountNumber | {{ MaskedAccountNumber }} | Returns the account's account number masked.Notes:Requires the user to select an account. |
| MasterBirthDate | {{ MasterBirthDate('MM-dd-yyyy') }} | Returns the business master user's birth date (if available) formatted with the provided format string. (Expression only applies to Business Master Users and Sub-users)Arguments:#1 - A constant string that is the format string for the user's birth date.Notes:The format string must adhere to C# DateTime format standards. |
| MasterDisplayName | {{ MasterDisplayName }} | Returns the business master user's display name. (Expression only applies to Business Master Users and Sub-users) |
| MasterFirstName | {{ MasterFirstName }} | Returns the business master user's first name. (Expression only applies to Business Master Users and Sub-users) |
| MasterLastName | {{ MasterLastName }} | Returns the business master user's last name. (Expression only applies to Business Master Users and Sub-users) |
| MasterMiddleName | {{ MasterMiddleName }} | Returns the business master user's middle name. (Expression only applies to Business Master Users and Sub-users) |
| MasterTaxId | {{ MasterTaxId }} | Returns the business master user's tax id (unsmaked). (Expression only applies to Business Master Users and Sub-users) |
| MasterUserId | {{ MasterUserId }} | Returns the business master user's Id. (Expression only applies to Business Master Users and Sub-users) |
| MasterUserIdentifier | {{ MasterUserIdentifier }} | Returns the master user's identifier. |
| MasterUserName | {{ MasterUserName }} | Returns the business master user's username. (Expression only applies to Business Master Users and Sub-users) |
| MemberIdentifier | {{ MemberIdentifier }} | Returns the user's member identifier. |
| MasterPrimaryPhone | {{ MasterPrimaryPhone }} | Returns the master user's primary phone. |
| MasterHomePhone | {{ MasterHomePhone }} | Returns the master user's home phone. |
| MasterMobilePhone | {{ MasterMobilePhone }} | Returns the master user's mobile phone. |
| MasterWorkPhone | {{ MasterWorkPhone }} | Returns the master user's work phone. |
| MasterEmail | {{ MasterEmail }} | Return the masters email |
| MasterUserExtendedProperty | {{ MasterUserExtendedProperty('PropertyName') }} | Allow for looking up an extended property associated to a master user.Arguments:#1 - A constant string that refers to the Extended Property Name. |
| MiddleName | {{ MiddleName }} | Returns the user's middle name. |
| MicrNumber | {{ MicrNumber }} | Returns Account.MicrNumber |
| MortgageNumber | {{ MortgageNumber }} | Returns the account's mortgage number (unmasked).Notes:Requires the user to select an account. |
| NextPaymentAmountDue | {{ NextPaymentAmountDue }} | Returns the account's next payment due amount (if applicable).Notes:Requires the user to select an account. |
| NextPaymentDate | {{ NextPaymentDate('MM-dd-yyyy') }} | Returns the account's next payment date (if applicable).Notes:Requires the user to select an account. |
| PadLeft | {{ PadLeft('Input', 10) }}-or-{{ PadLeft('Input', 10, '-') }} | Returns the provided string padded on the left to the minimum provided length.Arguments:#1 - A constant or an expression that will be padded.#2 - A constant string representing the number of characters to pad the string.The value must be greater than or equal to zero.#3 - (Optional) A constant string of a single character that will be used to pad (Default: <space>).Notes:If the pad length is less than the length of the input, the string is returned as is. |
| PadRight | {{ PadRight('Input', 10) }}-or-{{ PadRight('Input', 10, '-') }} | Returns the provided string padded on the right to the minimum provided length.Arguments:#1 - A constant or an expression that will be padded.#2 - A constant string representing the number of characters to pad the string.The value must be greater than or equal to zero.#3 - (Optional) A constant string of a single character that will be used to pad (Default: <space>).Notes:If the pad length is less than the length of the input, the string is returned as is. |
| PastDueAmount | {{ PastDueAmount }} | Returns the account's past due amount (if applicable).Notes:Requires the user to select an account. |
| PersonId | {{ PersonId }} | Returns the user's Person Id (i.e. "ExternalKey") from the user's extended properties. |
| PostalCode | {{ PostalCode }} | Returns the postal code of the user's address.Notes:The primary contact is preferred, if available. |
| PrimaryPhone | {{ PrimaryPhone }} | Returns the user's primary phone.Notes:If the user does not have any primary phone contacts, this will fallback to the user's main phone contact. |
| ProductCategory | {{ ProductCategory }} | Returns the product category provided as part of the inputs to call. |
| QueryString | {{ QueryString('ParameterName') }} | Allows for looking up query string parameters passed to the widgetArguments:#1 - A constant string that refers to the query string parameter name. |
| RandomString | {{ RandomsString(10) }} | Returns a random string of specified lengthArguments:#1 - A constant or an expression to specify the length of the resulting string. |
| Relationship | {{ Relationship }} | Returns the numeric value that represents the relationship associated to an account.Notes:Requires the user to select an account. |
| Replace | {{ Replace('Input', 'OldValue', 'NewValue') }} | Returns the string with the old value replaced by the new value.Arguments:#1 - A constant or an expression that will be modified.#2 - A constant or an expression that will be replaced within the original string (Argument #1).#3 - A constant or an expression that will replace the old value (Argument #2) in the string.Example: To replace the ':' with an empty string for the CoreName expression, the expression should be the following:"{{ Replace(CoreName, ':', '') }}"Original Value: S:0001 Result after Replace: S0001 |
| RegexReplace | {{ Replace('Input', 'Regex', 'ReplacementValue') }} | Returns the string with the old value replaced by the new value.Arguments:#1 - A constant or an expression that will be modified.#2 - A Regex expression, anything meeting it will be replaced by the 3rd value value.#3 - A constant or an expression that will replace the Regex matches in the string.Example: To replace the special characters with an empty string for the UserName expression, the expression should be the following:"{{ RegexReplace(UserName,'[^a-zA-Z0-9_.]+' , '') }}"Original Value: UserName123!@# Result after Replace: UserName123 |
| Right | {{ Right('Input', 4) }} | Returns the right-most characters from the provided string.Arguments:#1 - A constant or an expression that will be modified.#2 - A constant string that indicates how many characters will be returned.The value must be greater than zero.Notes:If the string is less than the number of desired characters, the string is returned as is. |
| Saml | {{ Saml('ServiceName') }} | Returns the result of the SAML service call. See the "Saml Service Example" on Standard-SSO.Arguments:#1 - A constant string that refers to the SAML Service's Key. |
| SessionId | {{ SessionId }} | Returns the identifier for the current session contained in the claims. |
| State | {{ State }} | Returns the state of the user's address.Notes:The primary contact is preferred, if available. |
| TaxId | {{ TaxId }} | Returns the user's tax id (unmasked).Notes:The format of the response will be 9 digits (e.g. "123456789").Tax IDs shorter than 9 digits will be padded with 0s on the front to 9 digits |
| Trim | {{ Trim('Input') }} | Returns the provided string trimmed at both the start and end of all white-space.Arguments:#1 - A constant or an expression that will be modified. |
| TrimEnd | {{ TrimEnd('Input') }} | Returns the provided string trimmed at the end of all white-space.Arguments:#1 - A constant or an expression that will be modified. |
| TrimStart | {{ TrimStart('Input') }} | Returns the provided string trimmed at the start of all white-space.Arguments:#1 - A constant or an expression that will be modified. |
| TransactionAmount | {{ TransactionAmount }} | Returns the Transaction amount.Notes:Requires the Transaction Id to be passed in - see Standard SSO - Transaction Integration |
| TransactionBatchNumber | {{ TransactionBatchNumber }} | Returns the Transaction batch number.Notes:Requires the Transaction Id to be passed in - see Standard SSO - Transaction Integration |
| TransactionCheckNumber | {{ TransactionCheckNumber }} | Returns the Transaction check number.Notes:Requires the Transaction Id to be passed in - see Standard SSO - Transaction Integration |
| TransactionCode | {{ TransactionCode }} | Returns the Transaction code.Notes:Requires the Transaction Id to be passed in - see Standard SSO - Transaction Integration |
| TransactionCoreSequence | {{ TransactionCoreSequence }} | Returns the Transaction core sequence.Notes:Requires the Transaction Id to be passed in - see Standard SSO - Transaction Integration |
| TransactionCreateDate | {{ TransactionCreateDate('MM-dd-yyyy') }} | Returns the Transaction's create date (if available) formatted with the provided format string.Arguments:#1 - A constant string that is the format string for the date the transaction was created.Notes:Requires the Transaction Id to be passed in - see Standard SSO - Transaction IntegrationThe format string must adhere to C# DateTime format standards. |
| TransactionEffectiveDate | {{ TransactionEffectiveDate('MM-dd-yyyy') }} | Returns the Transaction effective date (if available) formatted with the provided format string.Arguments:#1 - A constant string that is the format string for the effective date of the transaction.Notes:Requires the Transaction Id to be passed in - see Standard SSO - Transaction IntegrationThe format string must adhere to C# DateTime format standards. |
| TransactionImageSequence | {{ TransactionImageSequence }} | Returns the Transaction image sequence.Notes:Requires the Transaction Id to be passed in - see Standard SSO - Transaction Integration |
| TransactionImageSide | {{ TransactionImageSide }} | Returns the Transaction image side (used for Check Imaging).Notes:Requires the Transaction Id to be passed in - see Standard SSO - Transaction IntegrationRequires the ImageSide parameter to be passed in. |
| TransactionMerchant | {{ TransactionMerchant }} | Returns the Transaction merchant classification code.Notes:Requires the Transaction Id to be passed in - see Standard SSO - Transaction Integration |
| TransactionMicr | {{ TransactionMicr }} | Returns the MICR Account Number from the Transaction entry.Notes:Requires the Transaction Id to be passed in - see Standard SSO - Transaction Integration |
| TransactionPostingDate | {{ TransactionPostingDate('MM-dd-yyyy') }} | Returns the Transaction posting date (if available) formatted with the provided format string.Arguments:#1 - A constant string that is the format string for the posting date of the transaction.Notes:Requires the Transaction Id to be passed in - see Standard SSO - Transaction IntegrationThe format string must adhere to C# DateTime format standards. |
| TransactionPostingSequence | {{ TransactionPostingSequence }} | Returns the Transaction posting sequence.Notes:Requires the Transaction Id to be passed in - see Standard SSO - Transaction Integration |
| TransactionRoutingNumber | {{ TransactionRoutingNumber }} | Returns the Transaction Routing Number.Notes:Requires the Transaction Id to be passed in - see Standard SSO - Transaction IntegrationRequires the RoutingNumber parameter to be passed in. |
| TransactionSourceMicr | {{ TransactionSourceMicr }} | Returns the MICR Number from the Transaction parameters passed in.Notes:Requires the Transaction Id to be passed in - see Standard SSO - Transaction Integration |
| TransactionTraceNumber | {{ TransactionTraceNumber }} | Returns the Transaction trace number.Notes:Requires the Transaction Id to be passed in - see Standard SSO - Transaction Integration |
| Upper | {{ Upper('Input') }} | Returns the uppercase version of the provided string.Arguments:#1 - A constant or an expression that will be modified. |
| UrlEncode | {{ UrlEncode('Input') }} | Returns the URL encoded version of the provided string.Arguments:#1 - A constant or an expression that will be modified. |
| UserExtendedProperty | {{ UserExtendedProperty('PropertyName') }} | Allows for looking up an extended property associated to a user.Arguments:#1 - A constant string that refers to the Extended Property name. |
| UserId | {{ UserId }} | Returns the user's Id. |
| UserIdentifier | {{ UserIdentifier }} | Returns the user's identifier. |
| UserName | {{ UserName }} | Returns the username. |
| UtcDateTime | {{ UtcDateTime }} | Returns the current UTC DateTime in the ISO8601 format.Notes:Example response: 2020-06-23T22:48:04.9851859Z |
| WorkPhone | {{ WorkPhone }} | Returns the user's work phone.Notes:The primary contact is preferred, if available. |
| XPath | {{ XPath('MyExampleXml', '/My/Example/Path' }} | Retrieve the XML value for a given XML and path.Notes:#1 - XML value to be evaluated. The value can be an operation expression.#2 - XML path to the desired XML node value.Example: for XML <xml><test>alkami</test></xml> and path /xml/test, the value 'alkami' is returned. |
| XmlEscape | {{ XmlEscape('<test> Hello & Bye </test>') }} | Returns an XML escaped stringTakes on string argument |

1 The FI or Vendor must provide the value for this setting to Alkami before it can be enabled. Table Notes:

Please leave comments in the documents where you feel improvements can be made, and leave additional information or make changes as needed.
