INDEX: 91
ID: 89659272
TITLE: Adding additional features to the SDK
PARENT: How-to Articles
UPDATED: 2024-04-10
URL: https://confluence.alkami.com/spaces/SDKC/pages/89659272
-----
We are trying to keep the SDK installation limited to the minimum packages that are required for a viable environment. For that reason some features are not included in the basic setup package. This is due to:

- Some features that were previously in the basic SDK have been removed because they are not essential
- Some new features that have been released by Alkami are not essential
- As code is broken off into separate components it is not possible to leave them out of the basic install.

The following is a list of some features that have been identified as possibly useful to SDK users, along with a listing of the packages needed to install them.

### Content Management System (CMS)

```
choco install Alkami.MicroServices.CMS.Service.Host
choco install Alkami.Admin.Cms
choco install Alkami.Modules.Cms
```

### Package Assignments (Rules)

```
choco install Alkami.MicroServices.Rules.Service.Host
choco install Alkami.MicroServices.Rules.DataSheet.Service.Host

## Make sure these services are running.
```

### Client Fragment Manager (a useful replacement for using Html.Raw with dangerous content)

```
choco install Alkami.MicroServices.ClientFragmentManager.Service.Host
choco install Alkami.Admin.ClientFragmentManagement
choco install Alkami.Modules.ClientFragmentInjector
```
